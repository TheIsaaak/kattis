#include <iostream>
using namespace std;

int main() {
    int nodes;
    cin >> nodes;
    while (nodes--){
        int x,y,z;
        cin >> x >> y;
        cout << x-1 << endl;
        while (y--){
            cin >> x >> z;
        }
    }
}
